{
  "name": "worker",
  "sourceRoot": "apps/worker/src",
  "projectType": "application",
  "tags": [
    "scope:worker",
    "layer:composition",
    "runtime:cloud"
  ],
  "targets": {
    "build": {
      "command": "tsc -b apps/worker",
      "outputs": [
        "{projectRoot}/dist"
      ]
    }
  }
}
